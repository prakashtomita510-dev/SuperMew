{
  "name": "rewrite_dynamic_official",
  "kind": "rewrite",
  "dataset": "custom",
  "variant": "dynamic_rewrite",
  "seed": 42,
  "output_dir": "eval/outputs/rewrite",
  "params": {
    "rewrite_mode": "auto",
    "auto_merge_enabled": false,
    "auto_merge_threshold": 2
  },
  "tags": ["official", "comparison"]
}

{
  "name": "rewrite_no_rewrite_official",
  "kind": "rewrite",
  "dataset": "custom",
  "variant": "no_rewrite",
  "seed": 42,
  "output_dir": "eval/outputs/rewrite",
  "params": {
    "rewrite_mode": "off",
    "auto_merge_enabled": false,
    "auto_merge_threshold": 2
  },
  "tags": ["official", "baseline"]
}
